import { createBrowserRouter } from "react-router-dom";

const Routes = createBrowserRouter([
  {
    path: "/",
    element: <div className=" text-5xl font-black">Hello world!</div>,
  },
]);

export default Routes;
